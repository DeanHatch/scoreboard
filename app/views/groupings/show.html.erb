<p id="notice"><%= notice %></p>

<p>
  <strong>Groupingname:</strong>
  <%= @grouping.name %>
</p>

<p>
  <strong>Parent:</strong>
  <%= Grouping.find(@grouping.parent_id).name if @grouping.parent_id %>
</p>

<%= link_to 'Edit', edit_grouping_path(@grouping) %> |
<%= link_to 'Back', groupings_path %>

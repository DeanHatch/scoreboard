<h1><%= @competition.name %> <%= @competition.sport %> <%= @competition.variety %></h1>
<h2><%= @grouping.fullname %></h2>

<h3>
<%= (link_to 'Standings', ryandog_path(@competition.id, @grouping.id, "standings") ) if @grouping.has_teams?  %>
<%= (link_to 'Scores', ryandog_path(@competition.id, @grouping.id, "scores") ) if @grouping.has_teams? %>
<%= (link_to 'Schedule', ryandog_path(@competition.id, @grouping.id, "schedule") ) if @grouping.has_teams? %>
<%= (link_to 'Bracket', ryandog_path(@competition.id, @grouping.id, "bracket") ) if @grouping.bracket_grouping %>
</h3>
<br/>

<%=     render partial: @what_to_render %>

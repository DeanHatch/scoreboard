<%= form_for(@validdate) do |f| %>
  <% if @validdate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@validdate.errors.count, "error") %> prohibited this validdate from being saved:</h2>

      <ul>
      <% @validdate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Game Date' %><br>
    <%= f.date_select :gamedate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

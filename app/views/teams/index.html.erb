<h2> <%= @competition.name %>
<%= @competition.sport.humanize %> <%= @competition.variety.humanize %>
Teams</h2>
<h3> <%= @grouping.hierarchy.join(' - ') %> </h3>

<%= button_to 'New Team', new_grouping_team_path,
                                       method: :get %>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= link_to team.name, edit_team_path(team) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back to Groupings', groupings_path %>

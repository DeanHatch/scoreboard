<h1>Brackets for <%= @bracket.hierarchy.join(' ') %></h1>

<p id="notice"><%= notice %></p>

<p><%= @bracketcontests.count() %> Bracket Contests have been
 scheduled in this Bracket.</p>

<% needed = Bracketcontestant.all.select{|bc| bc.contest.bracket==@bracket}.
                       select{|bc| bc.contestanttype=="W"}.collect{|bc| bc.priorcontest} %> 

<% (@bracketcontests-needed).each do |bc| %> 
  <%= render partial: 'bracketcontests/bracket_contest' ,
       locals: { bracketcontest: bc, final: true, editable: true }  %>
  <br/>
<% end %>

<%= link_to 'Complete Bracket', complete_bracket_path(@bracket) %>
<br/>
<%= link_to 'New Bracketcontest', new_bracket_bracketcontest_path(@bracket) %>
<br/>
<%= link_to 'Back to Bracket Home', brackets_path %>

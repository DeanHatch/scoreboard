<h1>Manage a Competition</h1>
<h2>First, Which Customer&apos;s Competition is it?</h2>

<% Customer.all.each do |cust| %>
  <!-- %= link_to cust.name, choose_manager_competition_path(cust) % -->
  <%= link_to cust.name, choose_competition_manager_path(cust) %>
  <br/>
<% end %>

<%= form_for @manager,
                   url: change_manager_password_manager_path,
		   method: :patch do |f| %>
  <% if @manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manager.errors.count, "error") %> prohibited this competition from being saved:</h2>

      <ul>
      <% @manager.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :hashed_manager_password, 'Enter Password:' %>
    <%= f.password_field :manager_password, :size => 40 %>
  </div>
  <div class="field">
    <%= f.label :password_confirmation, 'Re-enter Password to Confirm:' %>
    <%= f.password_field :manager_password_confirmation, :size => 40 %>
  </div>
  <div class="actions">
    <%= f.submit  'Change Manager Password' %>
  </div>
<% end %>

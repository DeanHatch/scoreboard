<%= form_for @manager,
                   url: clear_manager_password_manager_path,
		   method: :patch do |f| %>
  <% if @manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manager.errors.count, "error") %> prohibited this competition from being saved:</h2>

      <ul>
      <% @manager.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="actions">
    <%= f.submit  'Clear (Remove) Manager Password' %>
  </div>
<% end %>
